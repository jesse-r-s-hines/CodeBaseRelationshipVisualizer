{
    "extends": "../../tsconfig.json",
    "compilerOptions": {
        "rootDir": "..", // root dir is still src so we can access shared.
        "lib": ["dom", "dom.iterable", "es2022"],
    },
    "include": [
        "**/*.ts"
    ]
}
